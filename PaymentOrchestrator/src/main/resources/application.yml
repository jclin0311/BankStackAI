server:
  port: 8086   
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AUTH0_ISSUER_URI:https://dev-4mxqf6pdu0czpsbp.us.auth0.com/}
          
      client:
        registration:
          account-m2m:
            provider: auth0
            authorization-grant-type: client_credentials
            # Auth0 machine-to-machine credentials used to call AccountService.
            # Supplied by the environment: .env locally (via direnv), the
            # bankstack-auth0 secret in Kubernetes. Startup fails if unset.
            client-id: ${AUTH0_M2M_CLIENT_ID}
            client-secret: ${AUTH0_M2M_CLIENT_SECRET}
            scope: admin:accounts

        provider:
          auth0:
            token-uri: ${AUTH0_ISSUER_URI:https://dev-4mxqf6pdu0czpsbp.us.auth0.com/}oauth/token
            
            
  application:
    name: payment-orchestrator

  datasource:
    url: jdbc:postgresql://localhost:5432/paymentdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update        # dev/demo ke liye; prod mein validate/none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

    consumer:
      group-id: ${PAYMENT_ORCH_CONSUMER_GROUP:payment-orch-consumers-v1}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

    listener:
      type: single  
      
      
payments:
  topics:
    billpay-requested: billpay.requested
    billpay-enqueued: billpay.enqueued
    bill-batch-ready: bill.batch.ready
    bill-batch-submitted: bill.batch.submitted
    billpay-status: billpay.status
    bill-batch-retry: bill.batch.retry
    bill-batch-resubmit: bill.batch.resubmit
    bill-batch-dlq: bill.batch.dlq
    
account:
  service:
    url: http://localhost:8084
    
    
biller:
  service:
    url: http://localhost:8088
    


auth0:
  audience: ${AUTH0_AUDIENCE:https://mockbank/api}




    
